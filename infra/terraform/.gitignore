# Terraform local state and caches — never commit these
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
.terraform.lock.hcl

# Local variable overrides (copy terraform.tfvars.example instead)
terraform.tfvars
*.tfvars.json